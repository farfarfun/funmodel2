# Changelog

## [Unreleased]

### Changed

- **Breaking**: renamed the import name and PyPI package name from `notemodel` to `funmodel2` to match the repository name (the repo name has always had a trailing "2" that the old import name never did). Anyone doing `import notemodel` or `pip install notemodel` must switch to `import funmodel2` / `pip install funmodel2`.
- The old `notemodel` PyPI package will get one final release forwarding to `funmodel2` (manual follow-up by the repo owner).
